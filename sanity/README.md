# Finlytics Sanity Studio

Sanity Studio for managing marketing and page content.

## Setup

1. Install deps: `pnpm install`
2. Run the studio: `pnpm dev`

## CORS

Allow your frontend origin:
`npx sanity cors add http://localhost:5173 --credentials`
